//day out of the year
let clock = moment().format('MMMM Do YYYY, h:mm:ss a');
$("currentDay").html(clock);


// Need to get current # of hours between 8-5 
// Need to make a custom array with 3 elements, hour, event, and save button in local storage
